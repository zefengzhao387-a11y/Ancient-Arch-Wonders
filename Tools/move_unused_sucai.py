# 已弃用：Unity 6 下 .meta 可能为 base64 guid，而 .scene 仍为 32 位 hex，
# 纯文本扫描会误判（曾导致「全部未使用」）。
# 请在 Unity 菜单执行：Tools / 素材 / Move Unused To 素材_未使用（可先 Dry Run）。

raise SystemExit("请使用 Unity Editor 菜单 Tools/素材/ 下的功能。")
